#!/usr/bin/env bash
# autostart.sh.example - Example autostart for bpswm
#
# Copy or symlink this to your bpswm autostart location (e.g., ~/.config/bpswm/autostart.sh)
#
# Launch the kawaii bar (adapt for lemonbar/polybar in real use)
$HOME/.dotfiles/themes/kawaii/bar.sh &
# Optionally, launch the settings app (for TUI/GUI settings)
# $HOME/.dotfiles/settings/settings.sh &
# ...add more startup commands as needed...
